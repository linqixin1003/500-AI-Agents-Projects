# 修复 Android 编译错误

## ❌ 错误信息

```
Plugin [id: 'org.jetbrains.kotlin.plugin.compose'] was not found
```

## ✅ 解决方案

`org.jetbrains.kotlin.plugin.compose` 插件在某些 Gradle 版本中不可用或需要特殊配置。

**已修复**：移除了该插件，Compose 功能仍然可以通过 `buildFeatures { compose = true }` 启用。

---

## 🔧 修改内容

### 修改前
```kotlin
plugins {
    id("org.jetbrains.kotlin.plugin.compose")  // ❌ 移除此行
}
```

### 修改后
```kotlin
plugins {
    // Compose 通过 buildFeatures 启用，不需要单独的插件
}
```

---

## ✅ 验证

重新同步 Gradle：

1. 在 Android Studio 中点击 "Sync Project with Gradle Files"
2. 或运行命令：
   ```bash
   cd diabeat-android
   ./gradlew build --refresh-dependencies
   ```

---

## 📋 其他可能的编译问题

### 问题 1：Kotlin 版本不匹配

如果仍有问题，检查 Kotlin 版本：

```kotlin
// build.gradle.kts (项目级)
plugins {
    id("org.jetbrains.kotlin.android") version "1.9.20" apply false
}
```

### 问题 2：Compose 版本不匹配

确保 Compose 版本与 Kotlin 版本兼容：

```kotlin
// app/build.gradle.kts
composeOptions {
    kotlinCompilerExtensionVersion = "1.5.4"
}
```

---

**修复完成**：现在应该可以正常编译了。

