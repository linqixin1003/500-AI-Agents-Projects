# 🚀 快速配置指南

## 您需要配置的 3 个必需项

### ✅ 1. DATABASE_URL（数据库连接）

**配置值**：
```env
DATABASE_URL=postgresql://diabeat:diabeat123@localhost:5432/diabeat
```

**说明**：
- 这是 `docker-compose.yml` 中的默认配置
- 如果使用 `docker-compose up -d db` 启动数据库，直接使用这个值即可
- 无需修改

---

### ✅ 2. SECRET_KEY（JWT 密钥）

**已为您生成**：
```env
SECRET_KEY=2b4c9104f9a9ed7a29bb1c29ce09b1e828b12e298c0d03af151c3cd5c77053c5
```

**说明**：
- 这是一个随机生成的密钥，用于 JWT Token 加密
- 直接使用这个值即可
- ⚠️ 不要使用默认值 `your-secret-key-change-in-production`

---

### ✅ 3. DASHSCOPE_API_KEY（通义千问 API Key）

**已从 rock-server 获取**：
```env
DASHSCOPE_API_KEY=sk-2969688c18494729b792af05b64ffbad
```

**说明**：
- 这是从 `rock-server` 的配置文件中获取的
- 直接使用这个值即可
- 用于食物图像识别功能

---

## 📝 配置步骤

### 第一步：创建配置文件

```bash
cd diabeat-server/config
touch .env
```

### 第二步：编辑配置文件

```bash
# 使用任意编辑器打开
vim .env
# 或
nano .env
# 或
code .env
```

### 第三步：复制以下内容

```env
# ============================================
# 必需配置
# ============================================

# 数据库连接
DATABASE_URL=postgresql://diabeat:diabeat123@localhost:5432/diabeat

# JWT 密钥（已生成）
SECRET_KEY=2b4c9104f9a9ed7a29bb1c29ce09b1e828b12e298c0d03af151c3cd5c77053c5

# 通义千问 API Key（从 rock-server 获取）
DASHSCOPE_API_KEY=sk-2969688c18494729b792af05b64ffbad

# ============================================
# 可选配置（AWS S3 已配置）
# ============================================

AWS_ACCESS_KEY_ID=AKIAW5WU5C4HN6FSSOFC
AWS_SECRET_ACCESS_KEY=UP61KnmqnaNSip0BBij/5HW7taNtyeb3ArupsDR
AWS_REGION=us-east-2
AWS_S3_BUCKET=diabeat-ai-images
S3_URL=https://diabeat-ai-images.s3.us-east-2.amazonaws.com

ENVIRONMENT=dev
HOST=localhost:8000
```

### 第四步：保存文件

保存后，配置完成！

---

## ✅ 配置检查

运行以下命令检查配置：

```bash
cd diabeat-server
python3 -c "
from app.config import settings
print('✅ DATABASE_URL:', '已设置' if settings.DATABASE_URL else '❌ 未设置')
print('✅ SECRET_KEY:', '已修改' if settings.SECRET_KEY != 'your-secret-key-change-in-production' else '❌ 未修改')
print('✅ DASHSCOPE_API_KEY:', '已设置' if settings.DASHSCOPE_API_KEY else '❌ 未设置')
print('✅ AWS S3:', '已配置' if settings.AWS_ACCESS_KEY_ID else '❌ 未配置')
"
```

---

## 🎯 配置总结

| 配置项 | 状态 | 说明 |
|--------|------|------|
| DATABASE_URL | ✅ 已提供 | 使用 docker-compose 默认值 |
| SECRET_KEY | ✅ 已生成 | 随机生成的密钥 |
| DASHSCOPE_API_KEY | ✅ 已获取 | 从 rock-server 获取 |
| AWS S3 | ✅ 已配置 | 之前已配置完成 |

**您只需要**：
1. 创建 `config/.env` 文件
2. 复制上面的配置内容
3. 保存文件

**完成！** 🎉

---

## 📚 更多信息

- 详细配置说明：`config/配置说明.md`
- 配置模板：`config/配置模板.txt`
- 启动指南：`启动指南.md`

---

**最后更新**：2025-11-06

